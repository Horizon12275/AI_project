server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/timegenie?createDatabaseIfNotExist=true
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
  application:
    name: user-service
  cloud: # 服务注册
    nacos:
      discovery:
        server-addr: localhost:8848